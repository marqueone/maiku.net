import React from "react";

import Header from "maiku.net/components/ui/header";
import Footer from "maiku.net/components/ui/footer";

const Layout = props => (
    <span>
        <Header info={props.info} />
        {props.children}
        <Footer />
        <div id="back-top"></div>
    </span>
)

export default Layout;