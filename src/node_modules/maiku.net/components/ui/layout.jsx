import React from "react";

import Header from "maiku.net/components/ui/header";
import Footer from "maiku.net/components/ui/footer";

const things = ["Software Developer", "Software Architect", "Traveler", "Amateur Photographer"]

const Layout = props => (
    <span>
        <Header things={things}/>
            {props.children}
        <Footer />
    </span>
);

export default Layout; 