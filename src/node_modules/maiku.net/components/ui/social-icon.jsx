import React from "react";

const SocialIcon = ({ icon, link }) => (
    <div className="ui-block-color06 social-network">
        <a href={link} className="hvr-sweep-to-right"><i className={`fa fa-${icon}`} aria-hidden="true"></i></a>
    </div>
);

export default SocialIcon;