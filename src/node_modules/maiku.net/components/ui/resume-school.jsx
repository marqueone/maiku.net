import React from "react";

const ResumeSchool = ({origin, duration, delay, distance, start, end, school, program, description}) => (
    <li>
        <div className="timeline-badge primary"><i className="flaticon-graduation61"></i></div>
        <div className="timeline-panel"
            data-animation-origin={origin && "left"}
            data-animation-duration={duration && "400"}
            data-animation-delay={delay && "400"}
            data-animation-distance={distance && "25px"}>
            <p className="timeline-time fontcolor-invert">{start} - {end}</p>
            <div className="timeline-photo timeline-mark-01"></div>
            <div className="timeline-heading">
                <h4 className="font-accident-two-normal uppercase">{school}</h4>
                <h6 className="uppercase">{program}</h6>
                <p>{description}</p>
            </div>
        </div>
    </li>
)

export default ResumeSchool;