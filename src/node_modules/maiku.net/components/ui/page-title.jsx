import React from "react";

const PageTitle = ({ origin, duration, delay, distance, name, title, info }) => (

    <section id="page-title" className="inner-section ui-block-color07">
        <div className="padding-50">
            <h2 className="font-accident-two-extralight color01 uppercase"
                data-animation-origin={origin && "right"}
                data-animation-duration={duration && "400"}
                data-animation-delay={delay && "100"}
                data-animation-distance={distance && "50px"}>{name}</h2>
            <h4 className="font-accident-two-normal color01 uppercase subtitle"
                data-animation-origin={origin && "right"}
                data-animation-duration={duration && "400"}
                data-animation-delay={delay && "100"}
                data-animation-distance={distance && "50px"}>{title}</h4>
            <p className="small color01"
                data-animation-origin={origin && "right"}
                data-animation-duration={duration && "400"}
                data-animation-delay={delay && "100"}
                data-animation-distance={distance && "50px"}>{info}</p>
        </div>
    </section>
);

export default PageTitle;