import React from "react";

const TimeLineProject = ({origin, duration, delay, distance, project, projectRole, description, tech, screenshot}) => (
    <li>
        <div className="timeline-badge primary"><i className="flaticon-businessman-in-apresentation-with-a-graphic-on-a-board"></i></div>
        <div className="timeline-panel"
            data-animation-origin={origin && "left"}
            data-animation-duration={duration && "400"}
            data-animation-delay={delay && "400"}
            data-animation-distance={distance && "25px"}>
            <div className="timeline-photo timeline-mark-01"></div>
            <div className="timeline-heading">
                <h4 className="font-accident-two-normal uppercase">{project}</h4>
                <h6 className="uppercase">{projectRole}</h6>
                <p>{description}</p>
                {tech && <h6 className="uppercase">{tech}</h6> }
            </div>
        </div>
    </li>
);

export default TimeLineProject;